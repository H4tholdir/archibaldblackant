# ====================================================================
# SECURITY WARNING: This is a template file with PLACEHOLDER values
# ====================================================================
# NEVER commit real credentials to this file!
# Real credentials should ONLY be in .env (which is gitignored)
# Copy this file to .env and replace placeholders with real values
#
# Setup: cp .env.example .env
# Then edit .env with your actual credentials
# ====================================================================

# Archibald ERP Configuration
# Your Archibald ERP server URL and authentication credentials
ARCHIBALD_URL=https://your-archibald-server/Archibald
ARCHIBALD_USERNAME=your_username_here
ARCHIBALD_PASSWORD=your_secure_password_here

# Server Configuration
# Port where the backend server will listen
PORT=3000
NODE_ENV=development

# Logging Configuration
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Redis Configuration (for BullMQ job queue)
# Redis server connection for background job processing
REDIS_HOST=localhost
REDIS_PORT=6379

# ====================================================================
# SECURITY BEST PRACTICES:
# - Use strong passwords (12+ characters, mixed case, numbers, symbols)
# - Never commit the .env file with real credentials
# - Rotate credentials immediately if accidentally exposed
# - Use environment-specific values in production (secrets manager)
# ====================================================================
