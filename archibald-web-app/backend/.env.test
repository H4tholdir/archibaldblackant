# Test environment overrides.
# PG_HOST is intentionally unset so describe.skipIf(!process.env.PG_HOST) skips
# DB integration tests when running locally without a database.
# CI sets PG_HOST explicitly to enable them.
PG_HOST=
