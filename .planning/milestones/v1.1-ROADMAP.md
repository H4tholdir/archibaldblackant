# Milestone v1.1: Full Feature Parity

**Status:** SHIPPED 2026-02-23
**Phases:** 8-10
**Total Plans:** 7

## Overview

Implementazione dei 5 elementi rimasti per parità funzionale completa con master — wiring DI mancante (reset checkpoint, test login, audit log), device registration con tracking al login, e sistema completo di price management con PostgreSQL migration.

## Phases

### Phase 8: Quick Wiring
**Goal**: Collegare 3 funzionalità che hanno già slot DI pronti — reset sync checkpoint, test login bot, audit log su invio ordine.
**Depends on**: v1.0 complete
**Plans**: 2 plans

Plans:
- [x] 08-01: Wire DI dependencies (resetSyncCheckpoint + createTestBot)
- [x] 08-02: Add audit log to send-to-verona handler

### Phase 9: Device Registration
**Goal**: Implementare il device tracking al login — repository CRUD per user_devices (tabella già esistente), hook nel login handler.
**Depends on**: Phase 8
**Plans**: 1 plan

Plans:
- [x] 09-01: Device repository + login hook wiring

### Phase 10: Price Management
**Goal**: Implementare il sistema completo di price management — migrazione price_history in PostgreSQL, PriceMatchingService per matching prezzi-prodotti, 4 endpoint fully functional.
**Depends on**: Phase 9
**Plans**: 4 plans

Plans:
- [x] 10-01: parseItalianPrice + matchVariant pure functions (TDD)
- [x] 10-02: Price History PostgreSQL migration + repository
- [x] 10-03: matchPricesToProducts service + wire stubs in server.ts
- [x] 10-04: sync-prices operation handler + final verification

---

## Milestone Summary

**Key Decisions:**
- createOperationQueue() reads Redis config from env vars, not config.ts
- Operation processor wired with 10 handler types via handler map pattern
- Sync scheduler: 10min agent, 30min shared intervals
- parseItalianPrice handles comma format (Italian locale)
- matchVariant uses exact article code + package size matching

**Issues Resolved:**
- DI wiring gaps for resetSyncCheckpoint and createTestBot
- Audit log missing on send-to-verona order handler
- Device registration not hooked into login flow
- Price history not migrated to PostgreSQL
- Price matching service not implemented

**Issues Deferred:**
- 13 stubs still in server.ts → v1.2
- Bootstrap/entry point for production → v1.2
- Subclient system, Fresis completion, admin sessions → v1.2

**Technical Debt Incurred:**
- None significant — clean implementations with TDD

---

_For current project status, see .planning/ROADMAP.md_
