---
phase: 15-dashboard-homepage-ui
plan: 01
type: execute
---

<objective>
Create homepage dashboard route with responsive layout and navigation to existing pages.

Purpose: Establish dashboard foundation with mobile-first responsive layout and navigation structure, enabling agents to access all app features from centralized homepage.
Output: Dashboard route at `/`, responsive 2-column grid layout, header with navigation links, mobile/tablet/desktop breakpoints tested.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@archibald-web-app/frontend/src/AppRouter.tsx
@archibald-web-app/frontend/src/App.css
@archibald-web-app/frontend/src/pages/OrderHistory.tsx
@archibald-web-app/frontend/src/components/OrderForm.tsx

**Tech stack available:**
- React 19 with hooks
- react-router-dom (BrowserRouter, Routes, Route, Navigate, useNavigate, useLocation)
- TypeScript strict mode
- Inline styles (Phase 10-06 decision)

**Established patterns:**
- Inline styles over CSS modules (Phase 10-06)
- Banking app UI patterns (Phase 10-06: color coding, temporal grouping)
- Functional components with hooks
- Mobile-first responsive design

**Constraining decisions:**
- Phase 10-06: Inline styles over CSS modules for consistency
- React-router-dom already configured in AppRouter.tsx
- Multiple pages exist: OrderHistory, PendingOrdersView, DraftOrders, CustomerList, AdminPage, ArticoliList

**Current routing structure:**
- AppRouter.tsx uses BrowserRouter with Routes/Route
- Existing routes: /orders, /pending, /drafts, /customers, /admin, /articoli
- Navigation currently via view state (not routes for OrderForm)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard homepage route with responsive layout</name>
  <files>
    archibald-web-app/frontend/src/pages/Dashboard.tsx,
    archibald-web-app/frontend/src/AppRouter.tsx
  </files>
  <action>
    Create Dashboard.tsx page component with:
    - Responsive layout: Mobile-first 2-column grid (1 column on mobile <768px, 2 columns on tablet/desktop >=768px)
    - Header section with user info (fullName from auth context)
    - Main content area with placeholder for widgets (3 empty div sections with borders for visual structure)
    - Inline styles following Phase 10-06 pattern
    - Use existing patterns from OrderHistory.tsx for structure

    Add Dashboard route to AppRouter.tsx:
    - Import Dashboard component
    - Add Route path="/" element={<Dashboard />} as first route in Routes
    - Ensure authenticated users land on dashboard by default

    Layout structure:
    - Container: max-width 1200px, centered, padding 20px
    - Grid: display grid, gap 20px, grid-template-columns 1fr on mobile, 1fr 1fr on desktop
    - Header: full-width grid-column span, display flex, justify space-between
    - Widget placeholders: border 2px dashed #ccc, padding 20px, min-height 200px

    Why inline styles: Consistent with Phase 10-06 decision, matches existing project convention from OfflineBanner and OrderHistory.
  </action>
  <verify>
    npm run typecheck in frontend directory passes

    Manual check:
    1. Start dev server: cd archibald-web-app/frontend && npm run dev
    2. Login and verify dashboard loads at /
    3. Verify 3 placeholder widget sections visible
    4. Check responsive behavior (DevTools mobile/tablet/desktop views)
  </verify>
  <done>
    Dashboard.tsx created with responsive 2-column grid layout.
    Dashboard route added to AppRouter.tsx at path="/".
    TypeScript compilation passes.
    Layout renders with 3 placeholder widget sections.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add navigation header with menu links</name>
  <files>
    archibald-web-app/frontend/src/components/DashboardNav.tsx,
    archibald-web-app/frontend/src/pages/Dashboard.tsx
  </files>
  <action>
    Create DashboardNav.tsx component with:
    - Horizontal navigation bar with links to all existing pages
    - Links: Dashboard (home icon), Ordini, Storico, Bozze, Pending, Clienti, Articoli, Admin
    - Use react-router-dom Link component for navigation
    - Active link highlighting (check useLocation().pathname)
    - Responsive: Horizontal scrollable on mobile, fixed bar on desktop
    - Inline styles: background #2c3e50, color white, padding 15px, display flex, gap 15px
    - Link styles: text-decoration none, color white, padding 10px 15px, border-radius 5px
    - Active link: background #3498db, font-weight bold

    Integrate DashboardNav in Dashboard.tsx:
    - Place DashboardNav at top of dashboard layout (before header section)
    - Full-width, sticky position top 0

    Navigation links map:
    - "/" → Dashboard (🏠 icon or "Home")
    - "/order-form" → Nuovo Ordine
    - "/orders" → Storico Ordini
    - "/drafts" → Bozze
    - "/pending" → Pending
    - "/customers" → Clienti
    - "/articoli" → Articoli
    - "/admin" → Admin

    Why Link not button: react-router-dom Link enables client-side navigation without page reload, consistent with existing routing patterns in AppRouter.tsx.
  </action>
  <verify>
    npm run typecheck passes

    Manual check:
    1. Verify navigation bar visible at top of dashboard
    2. Click each link and verify navigation to correct page
    3. Verify active link highlighting works
    4. Test responsive behavior on mobile (horizontal scroll if needed)
  </verify>
  <done>
    DashboardNav component created with 8 navigation links.
    Navigation integrated in Dashboard.tsx.
    Active link highlighting works via useLocation().
    TypeScript compilation passes.
    All links navigate correctly to existing pages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard homepage with responsive layout and navigation</what-built>
  <how-to-verify>
    1. Start dev server: cd archibald-web-app/frontend && npm run dev
    2. Login to app (if not already logged in)
    3. Verify dashboard loads at / with 3 placeholder widget sections
    4. Desktop (>768px):
       - Verify 2-column grid layout
       - Verify navigation bar horizontal with all 8 links visible
       - Verify user fullName displayed in header
    5. Tablet (768px):
       - Verify 2-column grid maintained
       - Verify navigation remains accessible
    6. Mobile (375px):
       - Verify 1-column grid layout (stacked widgets)
       - Verify navigation horizontal scrollable if needed
    7. Click each navigation link:
       - Dashboard → stays on /
       - Ordini → navigates to order form
       - Storico → navigates to /orders
       - Bozze → navigates to /drafts
       - Pending → navigates to /pending
       - Clienti → navigates to /customers
       - Articoli → navigates to /articoli
       - Admin → navigates to /admin
    8. Verify active link highlighting (current page link has blue background)
    9. Check no layout shift, no horizontal scroll (except navigation on mobile if needed)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes in frontend directory
- [ ] Dashboard.tsx created with responsive grid layout
- [ ] DashboardNav.tsx created with 8 navigation links
- [ ] Dashboard route added to AppRouter.tsx at path="/"
- [ ] No TypeScript errors
- [ ] User verification approved (responsive layout works on all breakpoints, navigation functional)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dashboard homepage renders at / with responsive 2-column grid
- Navigation bar with 8 links functional
- Active link highlighting works
- Mobile/tablet/desktop breakpoints tested and approved by user
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/15-dashboard-homepage-ui/15-01-SUMMARY.md`:

# Phase 15 Plan 01: Homepage Layout & Navigation Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `archibald-web-app/frontend/src/pages/Dashboard.tsx` - Description
- `archibald-web-app/frontend/src/components/DashboardNav.tsx` - Description
- `archibald-web-app/frontend/src/AppRouter.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 15-02-PLAN.md (Budget Progress Widget)
</output>
