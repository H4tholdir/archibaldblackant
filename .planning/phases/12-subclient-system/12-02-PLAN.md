---
phase: 12-subclient-system
plan: 02
type: execute
---

<objective>
Implement subclient Excel importer service with header variation mapping, code normalization, and bulk upsert/delete reconciliation.

Purpose: Master had subclient-excel-importer.ts that handled Excel import with 21 header variations, C-prefix normalization, and old record cleanup. This must be recreated following TDD with dependency injection for testability.
Output: Excel importer service with normalizeSubClientCode and importSubClients functions, wired to admin route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-subclient-system/12-01-SUMMARY.md

**Key Files:**
@archibald-web-app/backend/src/db/repositories/subclients.ts (upsertSubclients, getAllSubclients, deleteSubclientsByCodici from Plan 12-01)
@archibald-web-app/backend/src/routes/admin.ts (importSubclients stub to wire)
@archibald-web-app/backend/src/server.ts (admin route deps wiring)

**Master Implementation Details:**
- normalizeSubClientCode: handles C prefix (e.g., "C00123" -> "00123"), zero-padding to 5 digits
- importSubClientsFromExcel: reads Excel buffer via xlsx library, maps 21 header variations to canonical fields
- Header variations include: "Ragione Sociale" / "RAGIONE SOCIALE" / "ragione sociale" / "Rag. Sociale", "Codice" / "CODICE" / "Cod." / "COD.", etc.
- After import: compares new codici with existing, deletes records no longer in Excel
- Returns: { success, totalRows, inserted, updated, deleted }

**Established Patterns:**
- TDD: scaffold stub -> write failing test -> implement (C-1)
- Services use dependency injection for testability (pass deps object)
- xlsx library available in backend devDependencies
- Spec file colocated in same directory (T-1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subclient-excel-importer service with TDD</name>
  <files>
    archibald-web-app/backend/src/services/subclient-excel-importer.ts
    archibald-web-app/backend/src/services/subclient-excel-importer.spec.ts
  </files>
  <action>
    Follow TDD approach (C-1): write failing tests first, then implement.

    **Step 1: Write spec file (subclient-excel-importer.spec.ts)**

    Test `normalizeSubClientCode`:
    - Strips "C" prefix: "C00123" -> "00123"
    - Strips "c" prefix (case insensitive): "c00123" -> "00123"
    - Pads short codes: "123" -> "00123"
    - Already normalized: "00123" -> "00123"
    - Handles numeric input: 123 -> "00123"
    - Empty/null/undefined -> returns empty string or throws
    - Trims whitespace: " C00123 " -> "00123"

    Test `importSubClients`:
    - Successful import with valid Excel buffer (mock xlsx parse)
    - Maps all 21 header variations correctly
    - Skips rows with empty codice
    - Calls upsertSubclients with normalized data
    - Calls deleteSubclientsByCodici for removed records
    - Returns correct stats: { success: true, imported: N, skipped: N }
    - Handles empty Excel file gracefully
    - Handles invalid Excel buffer gracefully (returns { success: false, error: '...' })

    Use dependency injection pattern:
    ```typescript
    type ImportDeps = {
      upsertSubclients: (subclients: Subclient[]) => Promise<number>;
      getAllCodici: () => Promise<string[]>;
      deleteSubclientsByCodici: (codici: string[]) => Promise<number>;
    };

    type ImportResult = {
      success: boolean;
      imported?: number;
      skipped?: number;
      error?: string;
    };
    ```

    **Step 2: Implement service (subclient-excel-importer.ts)**

    ```typescript
    function normalizeSubClientCode(raw: string | number): string

    function importSubClients(
      buffer: Buffer,
      filename: string,
      deps: ImportDeps
    ): Promise<ImportResult>
    ```

    Header variation mapping (21 variations across 15 fields):
    - codice: "Codice", "CODICE", "Cod.", "COD.", "codice", "Cod"
    - ragioneSociale: "Ragione Sociale", "RAGIONE SOCIALE", "Rag. Sociale", "ragione sociale"
    - supplRagioneSociale: "Suppl. Rag. Sociale", "SUPPL. RAG. SOCIALE"
    - indirizzo: "Indirizzo", "INDIRIZZO"
    - cap: "CAP", "Cap", "C.A.P."
    - localita: "Localita", "LOCALITA", "Localit\u00e0"
    - prov: "Prov", "PROV", "Prov.", "Provincia"
    - telefono: "Telefono", "TELEFONO", "Tel.", "Tel"
    - fax: "Fax", "FAX"
    - email: "Email", "EMAIL", "E-mail", "e-mail"
    - partitaIva: "Partita IVA", "PARTITA IVA", "P.IVA", "P. IVA"
    - codFiscale: "Cod. Fiscale", "COD. FISCALE", "Codice Fiscale"
    - zona: "Zona", "ZONA"
    - persDaContattare: "Pers. da contattare", "PERS. DA CONTATTARE", "Persona da contattare"
    - emailAmministraz: "Email Amministraz.", "EMAIL AMMINISTRAZ.", "Email Amministrazione"

    Use case-insensitive header matching with trimming for robustness.

    Import flow:
    1. Parse Excel buffer using xlsx library
    2. Map headers to canonical field names
    3. Normalize codice values via normalizeSubClientCode
    4. Filter out rows without valid codice
    5. Call upsertSubclients with mapped data
    6. Get existing codici, compare with imported codici
    7. Delete codici that are in DB but not in imported Excel
    8. Return ImportResult with stats
  </action>
  <verify>
    npm test --prefix archibald-web-app/backend -- --grep subclient-excel
    npm run build --prefix archibald-web-app/backend
  </verify>
  <done>subclient-excel-importer service created with normalizeSubClientCode and importSubClients, all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Wire importSubclients stub in server.ts admin router</name>
  <files>
    archibald-web-app/backend/src/server.ts
    archibald-web-app/backend/src/routes/admin.ts
  </files>
  <action>
    1. **Check admin route for importSubclients stub:**
       - Locate the importSubclients stub in admin.ts or server.ts
       - Understand the expected signature and response format

    2. **Wire the import function in server.ts:**
       - Import importSubClients from services/subclient-excel-importer.ts
       - Import upsertSubclients, deleteSubclientsByCodici from db/repositories/subclients.ts
       - Create a wrapper function matching AdminRouterDeps signature: `(buffer: Buffer, filename: string) => Promise<ImportResult>`
       - The wrapper passes buffer, filename, and DI deps bound to pool:

       ```typescript
       const adminDeps = {
         importSubclients: async (buffer: Buffer, filename: string) => {
           return importSubClients(buffer, filename, {
             upsertSubclients: (subs) => upsertSubclients(pool, subs),
             getAllCodici: async () => {
               const all = await getAllSubclients(pool);
               return all.map(s => s.codice);
             },
             deleteSubclientsByCodici: (codici) => deleteSubclientsByCodici(pool, codici),
           });
         },
       };
       ```

    3. Verify admin route tests still pass.
  </action>
  <verify>
    npm test --prefix archibald-web-app/backend
    npm run build --prefix archibald-web-app/backend
  </verify>
  <done>importSubclients wired in server.ts admin route to real Excel importer service</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] normalizeSubClientCode handles C prefix, padding, case insensitivity, whitespace
- [ ] importSubClients parses Excel, maps 21 header variations, normalizes codes
- [ ] Import flow: upsert new + delete removed records
- [ ] ImportResult returns { success, imported, skipped, error? } matching AdminRouterDeps signature
- [ ] Dependency injection pattern used for testability (no direct DB calls in service)
- [ ] subclient-excel-importer.spec.ts covers all edge cases
- [ ] server.ts admin route wired to real importer
- [ ] `npm test --prefix archibald-web-app/backend` passes
- [ ] `npm run build --prefix archibald-web-app/backend` passes
</verification>

<success_criteria>
- normalizeSubClientCode correctly handles all code format variations
- importSubClients maps 21 Excel header variations to 15 canonical fields
- importSubClients accepts (buffer, filename, deps) matching admin route signature
- ImportResult returns { success, imported, skipped, error? } matching AdminRouterDeps
- Reconciliation: deletes DB records not present in imported Excel
- Dependency injection makes service fully unit-testable without DB
- Admin route importSubclients wired to real implementation
- Ready for production subclient Excel import workflow
</success_criteria>

<output>
After completion, create `.planning/phases/12-subclient-system/12-02-SUMMARY.md`:

# Phase 12 Plan 02: Subclient Excel Importer Summary

**Implemented Excel importer service with header variation mapping, code normalization, and reconciliation.**

## Accomplishments

- Created normalizeSubClientCode with C-prefix handling and zero-padding
- Implemented importSubClients with 21 header variation mapping
- Added reconciliation logic (delete records no longer in Excel)
- Wired admin route importSubclients to real service
- Full unit test coverage with dependency injection

## Files Created/Modified

- `archibald-web-app/backend/src/services/subclient-excel-importer.ts` - Importer service
- `archibald-web-app/backend/src/services/subclient-excel-importer.spec.ts` - Unit tests
- `archibald-web-app/backend/src/server.ts` - Wired admin deps
- `archibald-web-app/backend/src/routes/admin.ts` - Updated if needed

## Decisions Made

[Header matching strategy, reconciliation approach, error handling]

## Issues Encountered

[Any xlsx library issues, header edge cases, encoding problems]

## Next Step

Phase 12 (Subclient System) complete. Ready for Phase 13 (Fresis Completion).
</output>
