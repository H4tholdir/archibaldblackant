---
phase: 01-verification-test-infrastructure
plan: 01
type: execute
---

<objective>
Verify the feat/unified-operation-queue branch compiles, type-checks, and all existing tests pass. Parse mappa.pdf to extract the 42 redesigned elements and create a structured audit matrix mapping each element to its branch location and test coverage.

Purpose: Establish a verified baseline before auditing behavioral parity — ensures we know the current state of the branch and have a checklist for systematic verification.
Output: Build/test results documented + AUDIT-MATRIX.md with all 42 elements mapped.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md

Source document: /Users/hatholdir/Downloads/mappa.pdf (27 pages — full 1:1 comparison of 129 elements)

**Branch info:**
- Branch: feat/unified-operation-queue (62 commits ahead of master)
- Backend entry: archibald-web-app/backend/src/server.ts (DI factory pattern)
- Master entry: archibald-web-app/backend/src/index.ts (8,181-line monolith)
- Route modules: 16 files in archibald-web-app/backend/src/routes/
- Test files: 56 backend .spec.ts files, all using mocked dependencies
- Database: PostgreSQL (pg pool) — tests use mocked pool, no real DB needed
- Queue: BullMQ — tests use mocked queue, no Redis needed

**Codebase constraints:**
- Frontend type-check: `npm run type-check --prefix archibald-web-app/frontend`
- Backend type-check: `npm run build --prefix archibald-web-app/backend`
- Frontend tests: `npm test --prefix archibald-web-app/frontend`
- Backend tests: `npm test --prefix archibald-web-app/backend`

**Git note:** The .planning/ directory exists only on master. When working on the feature branch, either:
- Use `git worktree` to have both branches available
- Or cherry-pick the planning commits (c3b2f1c, 68a5db1, f474189) onto the feature branch
Choose the approach that works best operationally.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and test verification on feature branch</name>
  <files>archibald-web-app/backend/package.json, archibald-web-app/frontend/package.json</files>
  <action>
Switch to the feat/unified-operation-queue branch (use git worktree or checkout as appropriate — preserve .planning/ directory).

Install dependencies for both frontend and backend:
- `npm install --prefix archibald-web-app/frontend`
- `npm install --prefix archibald-web-app/backend`

Run type-check gates (per CLAUDE.md G-1):
- Frontend: `npm run type-check --prefix archibald-web-app/frontend`
- Backend: `npm run build --prefix archibald-web-app/backend`

Run test suites (per CLAUDE.md G-2):
- Frontend: `npm test --prefix archibald-web-app/frontend`
- Backend: `npm test --prefix archibald-web-app/backend`

Capture ALL output — pass counts, fail counts, error messages. If any failures occur, document them but do NOT attempt fixes yet (that's Plan 01-03).
  </action>
  <verify>Type-check commands run to completion (pass or fail documented). Test commands run to completion (pass/fail counts captured).</verify>
  <done>Build results, type-check results, and test results fully documented with pass/fail counts and any error details.</done>
</task>

<task type="auto">
  <name>Task 2: Create audit matrix from mappa.pdf</name>
  <files>/Users/hatholdir/Downloads/mappa.pdf, .planning/phases/01-verification-test-infrastructure/AUDIT-MATRIX.md</files>
  <action>
Read mappa.pdf (27 pages) to extract ALL elements marked as 🔄 RIPROGETTATO (redesigned). There should be approximately 42 elements.

For each redesigned element, determine:
1. **Element name** — the endpoint or feature (e.g., "POST /api/auth/login")
2. **Master location** — where it lives in master (typically index.ts with line range, or a route file)
3. **Branch location** — the corresponding file in the feature branch (e.g., routes/auth.ts)
4. **Has test** — whether a corresponding .spec.ts test exists on the branch
5. **Test status** — from Task 1 results: pass / fail / N/A

Create `.planning/phases/01-verification-test-infrastructure/AUDIT-MATRIX.md` with this structure:

```markdown
# Audit Matrix — 42 Redesigned Elements

## Summary
- Total redesigned elements: X
- With test coverage: Y
- Without test coverage: Z
- Tests passing: W
- Tests failing: F

## Matrix

| # | Element | Master Location | Branch Location | Has Test | Test Status | Notes |
|---|---------|----------------|-----------------|----------|-------------|-------|
| 1 | POST /api/auth/login | index.ts:L100-150 | routes/auth.ts | Yes | Pass | |
| ... | ... | ... | ... | ... | ... | |
```

Group elements by route module (auth, customers, orders, products, prices, sync, admin, warehouse, etc.).
Mark elements WITHOUT test coverage as priority targets for the code audit in Plan 01-02.
  </action>
  <verify>AUDIT-MATRIX.md exists with all 42 elements listed. Each element has all columns filled. Summary counts are correct.</verify>
  <done>Complete audit matrix with all 42 redesigned elements mapped to branch locations and test coverage status.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Feature branch builds without errors (or failures documented)
- [ ] Type-check passes for both frontend and backend (or errors documented)
- [ ] All tests run to completion with results captured
- [ ] AUDIT-MATRIX.md contains all ~42 redesigned elements
- [ ] Each element has: name, master location, branch location, test status
- [ ] Summary stats are accurate
</verification>

<success_criteria>

- Build and test baseline fully documented
- All 42 redesigned elements identified and mapped
- Test coverage gaps clearly visible
- Priority targets for code audit (Plan 01-02) identified
</success_criteria>

<output>
After completion, create `.planning/phases/01-verification-test-infrastructure/01-01-SUMMARY.md`
</output>
