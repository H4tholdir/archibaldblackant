---
phase: 02-critical-missing-endpoints
plan: 01
type: execute
---

<objective>
Implement POST /api/customers/smart-sync and POST /api/customers/resume-syncs endpoints on the feature branch. These endpoints manage sync pausing/resuming when agents open the order form.

Purpose: Critical production flow — when an agent opens the order form, smart-sync quickly refreshes customer data while pausing other syncs. When the form is closed, resume-syncs restarts background syncing.
Output: Two working endpoints with tests, matching master behavior.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-test-infrastructure/01-03-SUMMARY.md

**Branch info:**
- Feature branch: feat/unified-operation-queue
- All code changes go on the feature branch (use git worktree)
- Planning artifacts (SUMMARY, STATE, ROADMAP) go on master

**Master reference (index.ts lines):**
- POST /api/customers/smart-sync: L2273-2302
  - Calls `syncOrchestrator.smartCustomerSync()` — pauses other syncs, runs quick customer sync, resumes
  - Returns `{ success: true, message: "Smart Customer Sync completato" }`
- POST /api/customers/resume-syncs: L2305-2335
  - Calls `syncOrchestrator.resumeOtherSyncs()` — uses reference counting for multi-tab
  - Returns `{ success: true, message: "Syncs resumed" }`

**Branch architecture:**
- Routes: archibald-web-app/backend/src/routes/customers.ts
- Sync scheduler: archibald-web-app/backend/src/sync/sync-scheduler.ts
- DI pattern: all dependencies injected via factory function

**Key constraint:** The branch uses DI (not singletons). Check if `syncScheduler` has `smartCustomerSync()` and `resumeOtherSyncs()` methods. If not, they need to be added to the sync scheduler service.

**Test baseline:** 734 backend tests passing, 12 skipped. Do NOT break existing tests.

**Codebase constraints:**
- Backend type-check: `npm run build --prefix archibald-web-app/backend`
- Backend tests: `npm test --prefix archibald-web-app/backend`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement smart-sync and resume-syncs endpoints</name>
  <files>archibald-web-app/backend/src/routes/customers.ts, archibald-web-app/backend/src/sync/sync-scheduler.ts</files>
  <action>
Use git worktree: `git worktree add /tmp/archibald-feature-branch feat/unified-operation-queue`

1. Read master implementation:
   `git show master:archibald-web-app/backend/src/index.ts | sed -n '2273,2335p'`

2. Check if syncScheduler already has smart-sync/resume methods:
   Read `/tmp/archibald-feature-branch/archibald-web-app/backend/src/sync/sync-scheduler.ts`

3. If methods missing from syncScheduler, add them:
   - `smartCustomerSync()`: pause other syncs → run quick customer sync → resume
   - `resumeOtherSyncs()`: decrement pause reference count, resume if zero

4. Add routes to customers.ts (or sync-status.ts if more appropriate):
   - `POST /smart-sync` — calls syncScheduler.smartCustomerSync(), returns success message
   - `POST /resume-syncs` — calls syncScheduler.resumeOtherSyncs(), returns success message
   - Both require `authenticateJWT` middleware
   - Both have try/catch with 500 error response on failure

5. Register routes in server.ts if not already mounted

Follow master's response shape exactly:
- Success: `{ success: true, message: "Smart Customer Sync completato" }`
- Error: `{ success: false, error: error.message, message: "Errore durante..." }`
  </action>
  <verify>`npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes</verify>
  <done>Both endpoints exist, type-check passes, response shapes match master.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests and verify</name>
  <files>archibald-web-app/backend/src/routes/customers.spec.ts, archibald-web-app/backend/src/sync/sync-scheduler.spec.ts</files>
  <action>
1. Add unit tests for the new routes in customers.spec.ts (or sync-status.spec.ts):
   - Test POST /smart-sync returns 200 with success message
   - Test POST /smart-sync handles sync error gracefully (500)
   - Test POST /resume-syncs returns 200 with success message
   - Test POST /resume-syncs handles error gracefully (500)
   - Test both require authentication (401 without JWT)

2. If new methods added to sync-scheduler, add tests in sync-scheduler.spec.ts:
   - Test smartCustomerSync() calls correct methods in sequence
   - Test resumeOtherSyncs() decrements reference count correctly

3. Run full test suite: `npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend`

4. Commit on feature branch:
   ```
   cd /tmp/archibald-feature-branch
   git add [modified files individually]
   git commit -m "feat(02-01): implement smart-sync and resume-syncs endpoints

   - POST /api/customers/smart-sync pauses syncs for order form
   - POST /api/customers/resume-syncs resumes background syncing
   - Matches master behavior with DI architecture"
   ```
  </action>
  <verify>`npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes with no new failures. `npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes.</verify>
  <done>Tests pass, type-check passes, endpoints match master behavior. Commit on feature branch with hash recorded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] POST /api/customers/smart-sync endpoint works on feature branch
- [ ] POST /api/customers/resume-syncs endpoint works on feature branch
- [ ] Response shapes match master exactly
- [ ] Backend type-check passes
- [ ] All backend tests pass (no regressions from baseline 734+12)
- [ ] New tests cover success and error paths
</verification>

<success_criteria>

- Both endpoints implemented and tested
- Master behavior preserved (sync pause/resume semantics)
- Build + tests pass
- Code committed on feature branch
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-missing-endpoints/02-01-SUMMARY.md`
</output>
</content>
</invoke>