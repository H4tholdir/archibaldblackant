---
phase: 02-critical-missing-endpoints
plan: 04
type: execute
---

<objective>
Implement DELETE /api/sync/:type/clear-db endpoint on the feature branch. This admin-only endpoint resets database records for a specific sync type, enabling complete re-sync.

Purpose: When sync data becomes corrupted or out-of-date, admins need to clear a specific domain's data and force a complete re-sync. Critical for production troubleshooting.
Output: Working endpoint with tests, matching master behavior. Phase 2 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-test-infrastructure/01-03-SUMMARY.md

**Branch info:**
- Feature branch: feat/unified-operation-queue
- All code changes go on the feature branch (use git worktree)

**Master reference (index.ts lines):**
- DELETE /api/sync/:type/clear-db: L2547-2620
  - requireAdmin middleware
  - Valid types: ["customers", "products", "prices", "orders", "ddt", "invoices"]
  - Master approach: deletes the SQLite .db FILE from disk, forcing recreation on next sync
  - Returns: `{ success: true, message: "Database {type} cancellato con successo. Esegui una sync per ricrearlo." }`

**Branch architecture difference — CRITICAL:**
Master deletes SQLite files. Branch uses PostgreSQL with schemas (shared., agents.). The branch equivalent must:
1. TRUNCATE the relevant PostgreSQL tables (NOT delete files)
2. Determine which tables correspond to each sync type
3. Use transactions for consistency

**Table mapping (needs verification during execution):**
- customers → shared.customers (or agents.customers?)
- products → shared.products, shared.product_variations?
- prices → shared.prices, shared.price_history?
- orders → agents.order_records, agents.order_articles, agents.order_state_history?
- ddt → agents.ddt_records?
- invoices → agents.invoice_records?

Read the branch's migration files to determine exact table names:
`/tmp/archibald-feature-branch/archibald-web-app/backend/src/db/migrations/`

**Test baseline:** 734 backend tests passing, 12 skipped.

**Codebase constraints:**
- Backend type-check: `npm run build --prefix archibald-web-app/backend`
- Backend tests: `npm test --prefix archibald-web-app/backend`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement clear-db endpoint with PostgreSQL table truncation</name>
  <files>archibald-web-app/backend/src/routes/sync-status.ts, archibald-web-app/backend/src/db/repositories/</files>
  <action>
Use git worktree: `git worktree add /tmp/archibald-feature-branch feat/unified-operation-queue` (if not already created)

1. Read master implementation:
   `git show master:archibald-web-app/backend/src/index.ts | sed -n '2547,2620p'`

2. Read branch migration files to understand PostgreSQL schema:
   `ls /tmp/archibald-feature-branch/archibald-web-app/backend/src/db/migrations/`
   Read each migration to understand table structure and which tables belong to which sync type.

3. Determine table mapping for each sync type. Check repositories:
   ```
   ls /tmp/archibald-feature-branch/archibald-web-app/backend/src/db/repositories/
   ```
   Read each repository to understand table names.

4. Add a repository function `clearSyncData(pool, syncType)` that:
   - Maps sync type to table names
   - Uses TRUNCATE TABLE ... CASCADE within a transaction
   - Returns count of deleted records (optional)

5. Add route to sync-status.ts:
   - `DELETE /:type/clear-db` (mounted under /api/sync)
   - Middleware: `authenticateJWT, requireAdmin`
   - Validate type against ["customers", "products", "prices", "orders", "ddt", "invoices"]
   - Call clearSyncData repository function
   - Return `{ success: true, message: "Database {type} cancellato con successo. Esegui una sync per ricrearlo." }`

6. Error handling:
   - Invalid type → 400
   - Server error → 500 with error message
  </action>
  <verify>`npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes</verify>
  <done>DELETE /api/sync/:type/clear-db endpoint implemented with PostgreSQL table truncation. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests, verify full suite, complete Phase 2</name>
  <files>archibald-web-app/backend/src/routes/sync-status.spec.ts</files>
  <action>
1. Add tests in sync-status.spec.ts:
   - Test DELETE /:type/clear-db with valid type returns 200
   - Test requires admin (403 without admin role)
   - Test requires authentication (401 without JWT)
   - Test invalid type returns 400
   - Test each valid type is accepted
   - Test server error returns 500

2. Run full verification suite:
   `npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend`
   `npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend`
   `npm run type-check --prefix /tmp/archibald-feature-branch/archibald-web-app/frontend`
   `npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/frontend`

3. Commit on feature branch:
   ```
   cd /tmp/archibald-feature-branch
   git add [modified files individually]
   git commit -m "feat(02-04): implement DELETE /sync/:type/clear-db endpoint

   - Admin-only endpoint to reset sync data by type
   - PostgreSQL TRUNCATE instead of SQLite file deletion
   - Valid types: customers, products, prices, orders, ddt, invoices
   - Comprehensive test coverage"
   ```

4. Record all commit hashes from feature branch for SUMMARY.
  </action>
  <verify>All 4 verification gates pass (backend build, frontend type-check, backend tests, frontend tests). No regressions from baseline.</verify>
  <done>DELETE /api/sync/:type/clear-db works, tested. All Phase 2 endpoints implemented. Full test suite passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DELETE /api/sync/:type/clear-db endpoint works with all 6 sync types
- [ ] Admin middleware enforced
- [ ] PostgreSQL truncation works correctly
- [ ] Response shape matches master
- [ ] Backend type-check passes
- [ ] Frontend type-check passes
- [ ] All tests pass (no regressions)
- [ ] Phase 2 complete: all 4 plans executed
</verification>

<success_criteria>

- clear-db endpoint implemented and tested
- PostgreSQL table truncation replaces SQLite file deletion
- Admin authorization enforced
- Build + tests pass
- Phase 2 complete — all 6 critical missing endpoints implemented
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-missing-endpoints/02-04-SUMMARY.md`
</output>
</content>
