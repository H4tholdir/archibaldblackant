---
phase: 02-critical-missing-endpoints
plan: 02
type: execute
---

<objective>
Implement POST /api/orders/sync-states and GET /api/orders/resolve-numbers endpoints on the feature branch. sync-states is the most complex endpoint in this phase — it scrapes order states from Archibald ERP and propagates updates to fresis_history.

Purpose: Critical for order state tracking — agents need to see current order status (creato, piazzato, spedito, etc.) and map Archibald internal IDs to human-readable order numbers.
Output: Two working endpoints with tests, matching master behavior.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-test-infrastructure/01-03-SUMMARY.md

**Branch info:**
- Feature branch: feat/unified-operation-queue
- All code changes go on the feature branch (use git worktree)

**Master reference (index.ts lines):**
- POST /api/orders/sync-states: L7234-7400
  - Creates `OrderStateSyncService` instance
  - Calls `stateSyncService.syncOrderStates(userId, forceRefresh)`
  - Returns `{ success, message, data: { updated, unchanged, errors, cacheTimestamp, scrapedCount } }`
  - On success with updatedOrderIds: propagates state changes to fresis_history table
  - Propagation uses prepared statement with COALESCE for each order field
  - Catches propagation errors separately (warn, don't fail the whole request)

- GET /api/orders/resolve-numbers: L6062-6090
  - Query param `ids` (comma-separated, max 100)
  - Calls `orderDb.getOrderNumbersByIds(userId, ids)`
  - Returns `{ success: true, data: mappings }`
  - Simple batch lookup endpoint

**Branch architecture:**
- Orders route: archibald-web-app/backend/src/routes/orders.ts
- DB repos: archibald-web-app/backend/src/db/repositories/
- Branch uses PostgreSQL (pg pool), NOT SQLite — SQL queries need translation
- fresis_history propagation: check if fresis-history repo exists in branch

**Key complexity:** sync-states in master uses:
1. `OrderStateSyncService` — check if this exists in branch or needs creation
2. Direct SQLite access to users.db for fresis_history — branch uses PostgreSQL, needs different approach
3. `forceRefresh` query param for cache bypass

**Test baseline:** 734 backend tests passing, 12 skipped.

**Codebase constraints:**
- Backend type-check: `npm run build --prefix archibald-web-app/backend`
- Backend tests: `npm test --prefix archibald-web-app/backend`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement resolve-numbers endpoint</name>
  <files>archibald-web-app/backend/src/routes/orders.ts, archibald-web-app/backend/src/db/repositories/orders.ts</files>
  <action>
Use git worktree: `git worktree add /tmp/archibald-feature-branch feat/unified-operation-queue` (if not already created)

1. Read master implementation:
   `git show master:archibald-web-app/backend/src/index.ts | sed -n '6062,6090p'`

2. Check branch order repository for `getOrderNumbersByIds`:
   Read `/tmp/archibald-feature-branch/archibald-web-app/backend/src/db/repositories/orders.ts`
   - If method exists: use it
   - If missing: add it — PostgreSQL query: `SELECT archibald_order_id, order_number FROM orders WHERE user_id = $1 AND archibald_order_id = ANY($2)`

3. Add route to orders.ts:
   - `GET /resolve-numbers` (mounted under /api/orders)
   - Query param `ids` (comma-separated string, max 100)
   - Validate: ids required, string type, 1-100 items
   - Call repository method, return `{ success: true, data: mappings }`
   - Error: 400 for validation, 500 for server error

4. Add tests to orders.spec.ts:
   - Valid request with comma-separated IDs returns mappings
   - Missing ids param returns 400
   - Empty ids returns 400
   - More than 100 IDs returns 400

5. Commit on feature branch:
   ```
   cd /tmp/archibald-feature-branch
   git add [files individually]
   git commit -m "feat(02-02): implement GET /orders/resolve-numbers endpoint

   - Batch resolve Archibald IDs to order numbers
   - Validates 1-100 comma-separated IDs
   - PostgreSQL query with ANY($2) for batch lookup"
   ```
  </action>
  <verify>`npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes. Tests for resolve-numbers pass.</verify>
  <done>GET /api/orders/resolve-numbers works, validated, tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sync-states endpoint with fresis_history propagation</name>
  <files>archibald-web-app/backend/src/routes/orders.ts, archibald-web-app/backend/src/db/repositories/orders.ts, archibald-web-app/backend/src/db/repositories/fresis-history.ts</files>
  <action>
1. Read master implementation in detail:
   `git show master:archibald-web-app/backend/src/index.ts | sed -n '7234,7400p'`

2. Check if OrderStateSyncService exists in branch:
   `find /tmp/archibald-feature-branch/archibald-web-app/backend/src -name "*state*sync*" -o -name "*order*state*"`
   - If exists: use it
   - If missing: check what the service does in master and determine if we need to create it or can use existing sync services

3. Check fresis-history repository:
   Read fresis-history related files in branch DB repos
   - Need: method to propagate order state changes to fresis_history records
   - Master uses direct SQLite UPDATE with COALESCE for 15+ fields
   - Branch needs: PostgreSQL equivalent with parameterized query

4. Add route to orders.ts:
   - `POST /sync-states` (mounted under /api/orders)
   - Query param `forceRefresh=true` for cache bypass
   - Call sync service to scrape+update order states
   - If updatedOrderIds returned: propagate to fresis_history
   - Return `{ success, message, data: { updated, unchanged, errors, cacheTimestamp, scrapedCount } }`
   - Propagation errors: warn + continue (don't fail the request)

5. Add tests:
   - Test POST /sync-states returns correct response shape
   - Test forceRefresh param is passed to service
   - Test fresis_history propagation called on updates
   - Test propagation failure doesn't fail the request

6. Commit on feature branch:
   ```
   cd /tmp/archibald-feature-branch
   git add [files individually]
   git commit -m "feat(02-02): implement POST /orders/sync-states with fresis_history propagation

   - Sync order states from Archibald ERP
   - Propagate state changes to fresis_history for agent reporting
   - forceRefresh param for cache bypass
   - Propagation errors logged as warnings, don't fail request"
   ```

**IMPORTANT:** If OrderStateSyncService doesn't exist in branch and requires significant new service creation (scraping logic, caching), this is an architectural addition. In that case:
- Create a minimal service that delegates to the sync infrastructure already in place
- Do NOT replicate the master's singleton pattern — use DI
- The service should accept its dependencies via constructor/factory
  </action>
  <verify>`npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes with no regressions. `npm run build` passes.</verify>
  <done>POST /api/orders/sync-states works with fresis_history propagation. Response shape matches master. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GET /api/orders/resolve-numbers endpoint works
- [ ] POST /api/orders/sync-states endpoint works
- [ ] fresis_history propagation occurs on state updates
- [ ] Response shapes match master
- [ ] Backend type-check passes
- [ ] All backend tests pass (no regressions)
</verification>

<success_criteria>

- Both endpoints implemented and tested
- sync-states propagates to fresis_history correctly
- Master behavior preserved
- Build + tests pass
- Code committed on feature branch
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-missing-endpoints/02-02-SUMMARY.md`
</output>
</content>
