---
phase: 02-critical-missing-endpoints
plan: 03
type: execute
---

<objective>
Verify and complete the 5 interactive customer session endpoints (start, vat, heartbeat, save, delete) on the feature branch. These endpoints already exist as stubs (382 lines in customer-interactive.ts) — verify behavioral parity with master and fix any gaps.

Purpose: Interactive customer sessions allow agents to create customers in real-time through Puppeteer, with VAT lookup autofill. This is a critical production flow used daily.
Output: 5 verified endpoints matching master behavior, with comprehensive tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-test-infrastructure/01-03-SUMMARY.md

**Branch info:**
- Feature branch: feat/unified-operation-queue
- Existing stubs: archibald-web-app/backend/src/routes/customer-interactive.ts (382 lines, all 5 routes defined)
- Existing tests: archibald-web-app/backend/src/routes/customer-interactive.spec.ts

**Master reference (index.ts lines):**
- POST /api/customers/interactive/start: L3269-3395
  - Creates session, responds immediately with sessionId
  - Fire-and-forget: creates bot, navigates to customer form
  - Pauses background syncs during session
  - WebSocket progress: CUSTOMER_INTERACTIVE_PROGRESS events
- POST /api/customers/interactive/:sessionId/vat: L3397-3497
  - Calls `bot.submitVatAndReadAutofill(vatNumber)`
  - Returns autofill data from Archibald ERP
- POST /api/customers/interactive/:sessionId/heartbeat: L3499-3517
  - Extends session timeout
- POST /api/customers/interactive/:sessionId/save: L3520-3690
  - Calls `bot.completeCustomerCreation(formData)`
  - Creates customer in DB with upsertSingleCustomer
  - WebSocket progress events
  - Resumes syncs
- DELETE /api/customers/interactive/:sessionId: L3699-3735
  - Destroys session, releases bot
  - Resumes syncs if they were paused

**Branch stub analysis:**
- All 5 routes are defined in customer-interactive.ts
- DI pattern with CustomerInteractiveRouterDeps type
- Uses InteractiveSessionManager (check if fully implemented)
- Uses CustomerBotLike interface for bot operations
- Zod validation for vat and save schemas

**Key verification points:**
1. Does branch /start pause syncs correctly? (master pauses syncOrchestrator + priorityManager)
2. Does branch /save create customer in DB and emit WS progress?
3. Does branch /delete resume syncs correctly?
4. Is session timeout/heartbeat working?

**Test baseline:** 734 backend tests passing, 12 skipped.

**Codebase constraints:**
- Backend type-check: `npm run build --prefix archibald-web-app/backend`
- Backend tests: `npm test --prefix archibald-web-app/backend`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit interactive session stubs against master</name>
  <files>archibald-web-app/backend/src/routes/customer-interactive.ts</files>
  <action>
Use git worktree: `git worktree add /tmp/archibald-feature-branch feat/unified-operation-queue` (if not already created)

1. Read master implementation for all 5 endpoints:
   `git show master:archibald-web-app/backend/src/index.ts | sed -n '3269,3735p'`

2. Read branch stubs:
   `/tmp/archibald-feature-branch/archibald-web-app/backend/src/routes/customer-interactive.ts`

3. Compare each endpoint line-by-line:

   **POST /start:**
   - Master: creates session → responds 200 → fire-and-forget bot init → WS progress → navigate to form
   - Check branch does the same. Key: response BEFORE bot init (non-blocking)

   **POST /:sessionId/vat:**
   - Master: validates sessionId exists, bot ready, calls submitVatAndReadAutofill
   - Check branch has same validation and error handling

   **POST /:sessionId/heartbeat:**
   - Master: extends session timeout
   - Check branch has timeout extension

   **POST /:sessionId/save:**
   - Master: validates form data, calls bot.completeCustomerCreation OR bot.createCustomer (depending on session state), upserts customer in DB, WS progress, resumes syncs
   - Check branch has all steps

   **DELETE /:sessionId:**
   - Master: destroys session, removes bot, resumes syncs if paused
   - Check branch cleanup is complete

4. Document all differences found. For each:
   - If branch is missing behavior: fix it
   - If branch has extra behavior (DI improvements): keep it
   - If branch has wrong behavior: fix to match master

5. Apply fixes to customer-interactive.ts

6. Check InteractiveSessionManager:
   Read `/tmp/archibald-feature-branch/archibald-web-app/backend/src/interactive-session-manager.ts`
   Verify it has all methods used by the routes (createSession, destroySession, getActiveSessionForUser, removeBot, isSyncsPaused, markSyncsPaused, updateState, getSession, touchSession)
  </action>
  <verify>All 5 routes have complete implementation matching master behavior. No TODO/FIXME/stub comments remaining.</verify>
  <done>All interactive session endpoints verified against master, gaps fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests and run full suite</name>
  <files>archibald-web-app/backend/src/routes/customer-interactive.spec.ts</files>
  <action>
1. Read existing tests:
   `/tmp/archibald-feature-branch/archibald-web-app/backend/src/routes/customer-interactive.spec.ts`

2. Verify test coverage for all 5 endpoints:
   - POST /start: session creation, existing session cleanup, sync pausing
   - POST /:sessionId/vat: VAT lookup success, invalid session, bot not ready
   - POST /:sessionId/heartbeat: timeout extension
   - POST /:sessionId/save: form validation, customer creation, sync resume
   - DELETE /:sessionId: session cleanup, sync resume

3. Add any missing tests. Each endpoint should have at minimum:
   - Happy path test
   - Authentication required (401)
   - Invalid/missing session (404)
   - Error handling (500)

4. Run full test suite:
   `npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend`
   `npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend`

5. Commit on feature branch:
   ```
   cd /tmp/archibald-feature-branch
   git add [modified files individually]
   git commit -m "feat(02-03): verify and complete interactive customer session endpoints

   - Audit 5 endpoints against master implementation
   - Fix behavioral gaps found during comparison
   - Verify comprehensive test coverage
   - All tests pass"
   ```
  </action>
  <verify>`npm test --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes. `npm run build` passes. No regressions from baseline.</verify>
  <done>All 5 interactive session endpoints verified, tested, and committed. Build + tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 interactive session endpoints match master behavior
- [ ] InteractiveSessionManager fully functional
- [ ] Tests cover all endpoints (happy path + errors)
- [ ] Backend type-check passes
- [ ] All backend tests pass (no regressions)
</verification>

<success_criteria>

- 5 interactive session endpoints verified against master
- All behavioral gaps fixed
- Comprehensive tests passing
- Build + tests pass
- Code committed on feature branch
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-missing-endpoints/02-03-SUMMARY.md`
</output>
</content>
