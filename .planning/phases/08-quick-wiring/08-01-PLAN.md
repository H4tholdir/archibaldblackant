---
phase: 08-quick-wiring
plan: 01
type: execute
---

<objective>
Wire the two 501-degraded DI dependencies: resetSyncCheckpoint and createTestBot.

Purpose: Both endpoints exist with graceful 501 fallback. This plan provides real implementations so they return 200 instead of 501.
Output: `POST /api/sync/reset/:type` resets sync checkpoint, `POST /api/test/login` tests bot login against Archibald.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@archibald-web-app/backend/src/server.ts
@archibald-web-app/backend/src/server.spec.ts
@archibald-web-app/backend/src/routes/sync-status.ts
@archibald-web-app/backend/src/routes/sync-status.spec.ts
@archibald-web-app/backend/src/db/repositories/sync-sessions.ts
@archibald-web-app/backend/src/bot/archibald-bot.ts
@archibald-web-app/backend/src/config.ts

**Established patterns:**
- Optional DI with 501 fallback: `if (!deps.fn) return res.status(501).json(...)`
- clearSyncData as reference for standalone DI function (imported in server.ts, wired in syncStatusDeps)
- ArchibaldBot constructor: `new ArchibaldBot(userId, { browserPool, getUserById })`
- Bot has `initializeDedicatedBrowser()` for standalone browser (not from pool)

**Constraining decisions:**
- Phase 3 (03-03): resetSyncCheckpoint defined as optional DI in SyncStatusRouterDeps
- Phase 4 (04-03): createTestBot defined as optional DI in AppDeps
- DI pattern: server.ts is a factory that receives deps, not creates them
- sync_sessions table has CHECK constraint `sync_type = 'products'` — reset for customers/prices is a graceful no-op (0 rows updated)

**Test baseline:** 1213 backend + 441 frontend = 1654 passing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement and wire resetSyncCheckpoint</name>
  <files>archibald-web-app/backend/src/db/repositories/sync-sessions.ts, archibald-web-app/backend/src/server.ts</files>
  <action>
1. In `sync-sessions.ts`, add a `resetCheckpoint` function:
   - Signature: `async function resetCheckpoint(pool: DbPool, syncType: string): Promise<void>`
   - SQL: `UPDATE shared.sync_sessions SET status = 'failed', error_message = 'Checkpoint resettato manualmente', completed_at = $1 WHERE sync_type = $2 AND status IN ('running', 'partial')` with params `[Date.now(), syncType]`
   - For types without sync_sessions entries (customers, prices), the query updates 0 rows — this is correct behavior (graceful no-op)
   - Export `resetCheckpoint` from the module

2. In `server.ts`:
   - Import `resetCheckpoint` from `sync-sessions` repository (follow the pattern of `clearSyncData` import on line 41)
   - Add `resetSyncCheckpoint: (type: string) => resetCheckpoint(pool, type)` to `syncStatusDeps` object (line 498-507), casting type as needed to match `ResetSyncType`
   - Import the `ResetSyncType` type from `./routes/sync-status` if needed for the cast

Do NOT change the route handler in sync-status.ts — it already works correctly, just needs the dependency wired.
  </action>
  <verify>
Run: `npm test --prefix archibald-web-app/backend -- --run src/routes/sync-status.spec.ts`
All existing tests pass including the resetSyncCheckpoint tests (which previously used mocks — they should still pass since the router tests mock deps).
Run: `npm run build --prefix archibald-web-app/backend` — TypeScript compiles without errors.
  </verify>
  <done>
- `resetCheckpoint` function exists in sync-sessions.ts and is exported
- `syncStatusDeps` in server.ts includes `resetSyncCheckpoint` wired to the new function
- All sync-status tests pass
- TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement and wire createTestBot</name>
  <files>archibald-web-app/backend/src/server.ts, archibald-web-app/backend/src/bot/archibald-bot.ts</files>
  <action>
1. In `server.ts`, provide a default `createTestBot` implementation when `deps.createTestBot` is not supplied:
   - After destructuring deps (line 83-87), construct a default factory:
     ```
     const effectiveCreateTestBot = deps.createTestBot ?? (async () => {
       const bot = new ArchibaldBot('system-test', { browserPool });
       return {
         initialize: () => bot.initializeDedicatedBrowser(),
         login: () => bot.login(),
         close: () => bot.close(),
       };
     });
     ```
   - Import `ArchibaldBot` from `./bot/archibald-bot`
   - Use `effectiveCreateTestBot` in the endpoint handler (line 175-195) instead of `deps.createTestBot`
   - Remove the 501 check since we now always have a factory. BUT keep the 501 check only if browserPool is somehow unavailable — check: `if (!effectiveCreateTestBot)` should never be false now

   IMPORTANT: The test bot uses `initializeDedicatedBrowser()` (not `initialize()`) because:
   - `initialize()` acquires from pool and needs a valid cached session
   - `initializeDedicatedBrowser()` creates a standalone browser for one-off testing
   - Test login verifies the Archibald connection works, not pool health

   IMPORTANT: ArchibaldBot.login() reads credentials from `PasswordCache.get(userId)`. For 'system-test' userId there won't be cached credentials. Check how login() falls back — it may use `config.archibald.username/password` directly, or it may fail. If login() requires PasswordCache credentials:
   - Either set test credentials in PasswordCache before login: `PasswordCache.set('system-test', config.archibald.password)`
   - Or check if the bot has a way to login with explicit credentials
   - Read the `login()` method carefully (~line 2265 of archibald-bot.ts) to understand what it needs

2. Update tests in `server.spec.ts`:
   - The existing tests for createTestBot mock the deps. They should still pass because the mock overrides the default.
   - Add a test verifying the default factory is used when `createTestBot` is not in deps (should NOT return 501 anymore)

Do NOT modify archibald-bot.ts unless absolutely necessary for credential handling.
  </action>
  <verify>
Run: `npm test --prefix archibald-web-app/backend -- --run src/server.spec.ts`
All existing tests pass. The 501 test for createTestBot may need updating if we removed the 501 path.
Run: `npm run build --prefix archibald-web-app/backend` — TypeScript compiles without errors.
  </verify>
  <done>
- `createTestBot` has a default implementation in server.ts using ArchibaldBot + initializeDedicatedBrowser
- `POST /api/test/login` no longer returns 501 (always has a bot factory)
- All server.spec.ts tests pass (with updated 501 test if needed)
- TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --prefix archibald-web-app/backend` succeeds
- [ ] `npm test --prefix archibald-web-app/backend` — all tests pass (baseline: 1213)
- [ ] `npm run type-check --prefix archibald-web-app/frontend` — no regressions
- [ ] No new 501 responses for /api/sync/reset/:type and /api/test/login
</verification>

<success_criteria>

- resetSyncCheckpoint wired: endpoint returns 200 for valid types instead of 501
- createTestBot wired: endpoint uses default bot factory instead of returning 501
- All backend tests pass (≥1213)
- TypeScript compiles cleanly for both frontend and backend
</success_criteria>

<output>
After completion, create `.planning/phases/08-quick-wiring/08-01-SUMMARY.md`
</output>
