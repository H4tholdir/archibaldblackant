---
phase: 13-fresis-completion
plan: 01
type: execute
---

<objective>
Implement getNextFtNumber with PostgreSQL-backed counter and wire exportArca/importArca stubs in server.ts.

Purpose: Master had ft-counter.ts with a PostgreSQL ft_counter table (esercizio, user_id, last_number) for generating sequential FT document numbers. Master also had arca-export-service.ts for ZIP/DBF export and import. These must be recreated or wired on the branch.
Output: FT counter service with atomic increment, SQL migration, and wired export/import stubs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key Files:**
@archibald-web-app/backend/src/server.ts (stubs to wire: getNextFtNumber, exportArca, importArca)
@archibald-web-app/backend/src/routes/fresis-history.ts (routes using these deps)
@archibald-web-app/backend/src/routes/fresis-history.spec.ts (existing route tests)
@archibald-web-app/backend/src/db/repositories/fresis-history.ts (existing fresis repository)
@archibald-web-app/backend/src/db/migrations/006-subclients.sql (latest migration after Plan 12-01)

**Master ft-counter.ts Details:**
- Table: agents.ft_counter (esercizio TEXT, user_id TEXT, last_number INTEGER, PRIMARY KEY (esercizio, user_id))
- getNextFtNumber(pool, userId, esercizio): does INSERT ... ON CONFLICT DO UPDATE SET last_number = last_number + 1 RETURNING last_number
- Returns the next sequential number for a given user + fiscal year combination
- Atomic operation (no race conditions)

**Master arca-export-service.ts Details:**
- exportArca: creates ZIP containing DBF files (TESTATA.DBF, RIGHE.DBF, CLIENTI.DBF, DESTINAZIONI.DBF)
- importArca: parses ZIP with DBF files, extracts records
- Uses dbffile library for DBF read/write

**Established Patterns:**
- Repository functions accept DbPool (D-1)
- UPSERT with RETURNING for atomic operations
- TDD approach (C-1)
- Colocated spec files (T-1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration 007-ft-counter.sql and ft-counter service</name>
  <files>
    archibald-web-app/backend/src/db/migrations/007-ft-counter.sql
    archibald-web-app/backend/src/services/ft-counter.ts
    archibald-web-app/backend/src/services/ft-counter.spec.ts
  </files>
  <action>
    **Step 1: Create migration**

    ```sql
    CREATE TABLE IF NOT EXISTS agents.ft_counter (
      esercizio TEXT NOT NULL,
      user_id   TEXT NOT NULL,
      last_number INTEGER NOT NULL DEFAULT 0,
      PRIMARY KEY (esercizio, user_id)
    );
    ```

    **Step 2: Write spec file (ft-counter.spec.ts) - TDD**

    Test `getNextFtNumber`:
    - First call for new user+esercizio returns 1
    - Second call for same user+esercizio returns 2
    - Different esercizio starts fresh at 1
    - Different user_id starts fresh at 1
    - Concurrent calls produce unique sequential numbers (if testable)
    - Handles empty string esercizio/userId gracefully

    **Step 3: Implement ft-counter.ts**

    ```typescript
    import type { DbPool } from '../db/pool.js';

    async function getNextFtNumber(
      pool: DbPool,
      userId: string,
      esercizio: string
    ): Promise<number> {
      const result = await pool.query(
        `INSERT INTO agents.ft_counter (esercizio, user_id, last_number)
         VALUES ($1, $2, 1)
         ON CONFLICT (esercizio, user_id)
         DO UPDATE SET last_number = agents.ft_counter.last_number + 1
         RETURNING last_number`,
        [esercizio, userId]
      );
      return result.rows[0].last_number;
    }
    ```

    Export the function for use in server.ts wiring.
  </action>
  <verify>
    npm test --prefix archibald-web-app/backend -- --grep ft-counter
    npm run build --prefix archibald-web-app/backend
  </verify>
  <done>Migration 007-ft-counter.sql and ft-counter service created with atomic getNextFtNumber, tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Wire getNextFtNumber, exportArca, and importArca in server.ts</name>
  <files>
    archibald-web-app/backend/src/server.ts
  </files>
  <action>
    1. **Wire getNextFtNumber:**
       - Import getNextFtNumber from services/ft-counter.ts
       - Replace the stub in server.ts FresisHistoryRouterDeps:
         ```typescript
         getNextFtNumber: (userId, esercizio) => getNextFtNumber(pool, userId, esercizio),
         ```

    2. **Wire exportArca:**
       - Check if arca-export-service.ts exists on branch
       - If YES: import and wire it directly
       - If NO: create a minimal stub that returns a proper error message indicating "not yet implemented" but with correct type signature so TypeScript compiles
       - The stub should match the expected signature from FresisHistoryRouterDeps

    3. **Wire importArca:**
       - Same approach as exportArca: check existence, wire if available, stub with proper types if not
       - The stub should accept a ZIP buffer and return a "not yet implemented" result

    4. Verify all fresis-history route tests still pass.
    5. Verify TypeScript compilation succeeds.
  </action>
  <verify>
    npm test --prefix archibald-web-app/backend
    npm run build --prefix archibald-web-app/backend
  </verify>
  <done>getNextFtNumber wired to real service, exportArca/importArca wired or properly stubbed with correct types</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration 007-ft-counter.sql creates agents.ft_counter with composite primary key (esercizio, user_id)
- [ ] getNextFtNumber uses atomic INSERT ON CONFLICT DO UPDATE RETURNING
- [ ] ft-counter.spec.ts tests sequential increment, isolation by user/esercizio
- [ ] server.ts getNextFtNumber stub replaced with real call
- [ ] exportArca/importArca wired or properly typed stubs in place
- [ ] `npm test --prefix archibald-web-app/backend` passes
- [ ] `npm run build --prefix archibald-web-app/backend` passes
- [ ] No race condition possible in FT number generation
</verification>

<success_criteria>
- FT counter atomically generates sequential numbers per user per fiscal year
- No race conditions: uses PostgreSQL UPSERT with RETURNING
- getNextFtNumber fully tested with TDD approach
- exportArca/importArca at minimum have proper type stubs that compile
- Fresis history routes can call getNextFtNumber without errors
- Ready for Plan 13-02 (Missing Endpoints + Discount Bulk Upload)
</success_criteria>

<output>
After completion, create `.planning/phases/13-fresis-completion/13-01-SUMMARY.md`:

# Phase 13 Plan 01: getNextFtNumber + exportArca + importArca Summary

**Implemented atomic FT counter service with PostgreSQL-backed sequential numbering and wired export/import stubs.**

## Accomplishments

- Created SQL migration 007-ft-counter.sql for agents.ft_counter table
- Implemented getNextFtNumber with atomic UPSERT + RETURNING pattern
- Wrote unit tests covering sequential increment and user/esercizio isolation
- Wired getNextFtNumber in server.ts to real service
- Handled exportArca/importArca (wired or typed stubs)

## Files Created/Modified

- `archibald-web-app/backend/src/db/migrations/007-ft-counter.sql` - Migration
- `archibald-web-app/backend/src/services/ft-counter.ts` - FT counter service
- `archibald-web-app/backend/src/services/ft-counter.spec.ts` - Unit tests
- `archibald-web-app/backend/src/server.ts` - Wired dependencies

## Decisions Made

[exportArca/importArca approach, counter reset strategy]

## Issues Encountered

[Any migration ordering issues, DBF library availability]

## Next Step

Ready for Plan 13-02 (Missing Endpoints + Discount Bulk Upload).
</output>
