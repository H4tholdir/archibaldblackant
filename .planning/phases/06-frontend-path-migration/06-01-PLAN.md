---
phase: 06-frontend-path-migration
plan: 01
type: execute
---

<objective>
Migrate remaining frontend API calls from legacy order paths to unified operations endpoints and clean up dead code.

Purpose: Complete the frontend path migration so all API calls use the unified operation queue routes, enabling future removal of legacy backend routes.
Output: All frontend API calls use unified paths, no dead code remains, all tests pass.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stubs-partial-completion/05-01-SUMMARY.md

# Key files to modify:
@archibald-web-app/frontend/src/components/OrderStatus.tsx
@archibald-web-app/frontend/src/hooks/usePendingSync.ts
@archibald-web-app/frontend/src/api/products.ts

# Reference for new endpoint behavior:
@archibald-web-app/backend/src/routes/operations.ts

**Tech stack available:** Express, React, fetchWithRetry (auto JWT injection), Vitest
**Established patterns:** fetchWithRetry for all API calls, `{ success, data/job }` response shape

**Constraining decisions:**
- 01-02: Response shape changes (sync->jobId) deferred to Phase 6 frontend migration
- 02-02: Operations endpoint returns `{ success: true, job }` not `{ success: true, data }`

**Migration status (from discovery):**
Most roadmap items already migrated during Phases 1-5:
- auth/me → auth/verify: NOT applicable (backend still uses /me, no /verify endpoint)
- orders/my-orders → operations/user/:userId: Already migrated
- queue/stats → operations/stats: Already migrated
- customers/search → customers?search=: Already migrated
- products/search → products?search=: Already migrated in service layer
- edit/delete-in-archibald → operations/enqueue: Already migrated

**Remaining migrations:**
1. orders/status/:jobId → operations/:jobId/status (2 files + response shape change)
2. Dead `searchProducts` function in api/products.ts (unused, all callers use ProductService)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate order status polling and remove dead code</name>
  <files>
    archibald-web-app/frontend/src/components/OrderStatus.tsx,
    archibald-web-app/frontend/src/hooks/usePendingSync.ts,
    archibald-web-app/frontend/src/api/products.ts
  </files>
  <action>
    **OrderStatus.tsx (line 27):**
    Change `/api/orders/status/${jobId}` to `/api/operations/${jobId}/status`.
    Update response shape: the operations endpoint returns `{ success: true, job: {...} }` instead of `{ success: true, data: {...} }`.
    - Line 31: `data.data` → `data.job`
    - Line 34: `data.data.status` → `data.job.status`

    **usePendingSync.ts (line 101):**
    Change `/api/orders/status/${order.jobId}` to `/api/operations/${order.jobId}/status`.
    Update response shape:
    - Line 108: `data.data?.status` → `data.job?.status`

    Auth headers are automatically injected by `fetchWithRetry`, so no auth changes needed.

    **api/products.ts (searchProducts function, lines 249-271):**
    Remove the entire `searchProducts` function. It is dead code — all callers use `ProductService.searchProducts` from `products.service.ts` which already uses the new `/api/products?search=` path. Also remove the `SearchResponse` type if it's only used by `searchProducts`. Do NOT remove any other exports from this file (getProducts, getProductVariants, updateProductVat, updateProductPrice, etc. are still used).
  </action>
  <verify>
    npm run type-check --prefix archibald-web-app/frontend
  </verify>
  <done>
    - OrderStatus.tsx calls /api/operations/:jobId/status and reads response.job
    - usePendingSync.ts calls /api/operations/:jobId/status and reads response.job
    - searchProducts function removed from api/products.ts
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify migration completeness and run full test suite</name>
  <files>
    archibald-web-app/frontend/src/**,
    archibald-web-app/backend/src/**
  </files>
  <action>
    **Verify no remaining old API paths in frontend:**
    Grep the entire frontend/src directory for these patterns that should NOT exist:
    - `/api/orders/status/` (should be /api/operations/.../status)
    - `/api/products/search` (should be /api/products?search=)
    - `/api/customers/search` (should be /api/customers?search=)
    - `/api/queue/stats` (should be /api/operations/stats)
    - `edit-in-archibald` (should be operations/enqueue)
    - `delete-from-archibald` (should be operations/enqueue)
    - `orders/my-orders` (should be operations/user/:userId)

    If any matches found, fix them following the same pattern.

    **Run all tests:**
    - Frontend: `npm test --prefix archibald-web-app/frontend`
    - Backend: `npm test --prefix archibald-web-app/backend`

    **Run type checks:**
    - Frontend: `npm run type-check --prefix archibald-web-app/frontend`
    - Backend: `npm run build --prefix archibald-web-app/backend`
  </action>
  <verify>
    All grep searches return 0 matches.
    Frontend tests pass (baseline: 418).
    Backend tests pass (baseline: 881).
    Both type checks pass.
  </verify>
  <done>
    - No legacy API paths remain in frontend source code
    - All frontend tests pass
    - All backend tests pass
    - TypeScript compiles for both frontend and backend
    - Phase 6 complete
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run type-check --prefix archibald-web-app/frontend` passes
- [ ] `npm run build --prefix archibald-web-app/backend` passes
- [ ] `npm test --prefix archibald-web-app/frontend` passes (baseline: 418)
- [ ] `npm test --prefix archibald-web-app/backend` passes (baseline: 881)
- [ ] No legacy order/product/customer paths in frontend grep results
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No legacy API paths remain in frontend
- Response shape changes applied correctly (data → job)
- No dead code remaining
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-path-migration/06-01-SUMMARY.md`
</output>
