---
phase: 06-frontend-path-migration
plan: 02
type: execute
---

<objective>
Verify all planned frontend path migrations are complete and close Phase 6 by updating the roadmap.

Purpose: The discovery in 06-01 revealed that most migrations (06-02 and 06-03 roadmap items) were already completed during Phases 1-5. This plan performs a comprehensive verification sweep and formally closes Phase 6.
Output: Verified migration completeness, updated roadmap marking all Phase 6 plans complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-path-migration/06-01-SUMMARY.md

**Tech stack available:** Express, React, fetchWithRetry (auto JWT injection), Vitest
**Established patterns:** fetchWithRetry for all API calls, `{ success, data/job }` response shape

**Constraining decisions:**
- 06-01: Response shape change data.data → data.job applied for operations endpoints
- 06-01: auth/me NOT migrated (backend has no /auth/verify endpoint — intentional)

**Migration map (all 8 items from roadmap):**
1. ~~auth/me → auth/verify~~ — NOT applicable (backend uses /me, no /verify exists)
2. orders/status → operations/:jobId/status — ✓ Migrated in 06-01
3. orders/my-orders → operations/user/:userId — ✓ Already migrated before Phase 6
4. queue/stats → operations/stats — ✓ Already migrated before Phase 6
5. customers/search → customers?search= — ✓ Already migrated before Phase 6
6. products/search → products?search= — ✓ Already migrated before Phase 6
7. edit-in-archibald → operations/enqueue — ✓ Already migrated before Phase 6
8. delete-from-archibald → operations/enqueue — ✓ Already migrated before Phase 6
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive legacy path verification</name>
  <files>archibald-web-app/frontend/src/**</files>
  <action>
    Run grep against the entire frontend/src directory for ALL legacy API path patterns that should NOT exist after migration. Each pattern must return 0 matches:

    1. `/api/orders/status/` — should be /api/operations/:jobId/status
    2. `/api/orders/my-orders` — should be /api/operations/user/:userId
    3. `/api/queue/stats` — should be /api/operations/stats
    4. `/api/products/search` — should be /api/products?search=
    5. `/api/customers/search` — should be /api/customers?search=
    6. `edit-in-archibald` — should be operations/enqueue
    7. `delete-from-archibald` — should be operations/enqueue

    Also verify that the ONE intentionally-kept legacy path is documented:
    8. `/api/auth/me` — expected in auth.ts (backend has /auth/me, no /auth/verify endpoint)

    If any unexpected matches are found, fix them following the patterns established in 06-01 (use unified operations paths, update response shapes if needed).
  </action>
  <verify>
    All 7 legacy pattern greps return 0 matches.
    `/api/auth/me` grep returns exactly 1 match in api/auth.ts (intentional).
  </verify>
  <done>
    - All 7 legacy path patterns confirmed absent from frontend
    - auth/me documented as intentionally kept
    - Zero migration gaps found
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and update roadmap</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
    **1. Run full test suite to confirm baseline:**
    - Frontend: `npm test --prefix archibald-web-app/frontend`
    - Backend: `npm test --prefix archibald-web-app/backend`
    - Frontend type-check: `npm run type-check --prefix archibald-web-app/frontend`
    - Backend type-check: `npm run build --prefix archibald-web-app/backend`

    **2. Update ROADMAP.md:**
    - Mark 06-02 as complete with checkmark `[x]`
    - Mark 06-03 as complete with checkmark `[x]`
    - Update 06-02 description to: "Verification sweep — all queue/stats, customers/search, products/search migrations confirmed complete"
    - Update 06-03 description to: "Verification sweep — all edit/delete-in-archibald → operations/enqueue migrations confirmed complete"
    - Update Phase 6 progress row: `3/3 | Complete | 2026-02-23`

    **3. Update STATE.md:**
    - Update current position: Phase 6 complete, Phase 7 next
    - Update progress percentage to reflect Phase 6 completion (17/20 plans)
    - Update last activity date
    - Update session continuity to point to Phase 7
    - Add velocity entry for Phase 6 plan 02
  </action>
  <verify>
    Frontend tests pass (baseline: 418).
    Backend tests pass (baseline: 881).
    Both type-checks pass.
    ROADMAP.md shows Phase 6 as Complete.
    STATE.md reflects Phase 7 as next.
  </verify>
  <done>
    - All 1299 tests passing
    - TypeScript compiles for both frontend and backend
    - ROADMAP.md updated: Phase 6 fully complete (3/3 plans)
    - STATE.md updated: ready for Phase 7
    - Phase 6 formally closed
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run type-check --prefix archibald-web-app/frontend` passes
- [ ] `npm run build --prefix archibald-web-app/backend` passes
- [ ] `npm test --prefix archibald-web-app/frontend` passes (baseline: 418)
- [ ] `npm test --prefix archibald-web-app/backend` passes (baseline: 881)
- [ ] All 7 legacy API path greps return 0 matches in frontend
- [ ] ROADMAP.md shows Phase 6 complete (3/3)
- [ ] STATE.md points to Phase 7 as next
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- All 8 migration items accounted for (7 confirmed migrated, 1 intentionally kept)
- Phase 6 complete in roadmap
- STATE.md ready for Phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-path-migration/06-02-SUMMARY.md`
</output>
