---
phase: 03-admin-monitoring-endpoints
plan: 01
type: execute
---

<objective>
Implement GET /api/sync/quick-check and POST /api/admin/sync/frequency endpoints. Verify that existing GET /api/sync/intervals and POST /api/sync/intervals/:type match master behavior.

Purpose: quick-check tells the frontend if initial sync is needed on app load. sync/frequency provides a simplified admin interface to change all sync intervals at once.
Output: Endpoints implemented and verified, matching master behavior.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-critical-missing-endpoints/02-04-SUMMARY.md

**Branch info:**
- Feature branch: feat/unified-operation-queue (latest: 34ba13c)
- Existing: GET /intervals (L173) and POST /intervals/:type (L193) already in sync-status.ts
- Missing: GET /quick-check, POST /admin/sync/frequency

**Master reference:**
- GET /api/sync/quick-check (L4514-4567): No auth, checks customer/product counts and lastSync times. Returns needsSync boolean + per-type details.
- POST /api/admin/sync/frequency (L2824-2857): Sets global sync interval (5-1440 min). Restarts scheduler. No auth in master (TODO noted). Branch should add requireAdmin.
- GET /api/sync/intervals (L4919-4938): Returns intervals for all 6 types. requireAdmin.
- POST /api/sync/intervals/:type (L4940-4980): Updates interval for one type (5-1440 min). requireAdmin.

**Branch existing intervals endpoints:** Already implemented at sync-status.ts L173 and L193. Need verification only.

**Test baseline:** 796 backend + 418 frontend = 1214 passing, 12 skipped.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement quick-check and admin/sync/frequency endpoints</name>
  <files>archibald-web-app/backend/src/routes/sync-status.ts, archibald-web-app/backend/src/routes/admin.ts, archibald-web-app/backend/src/server.ts</files>
  <action>
Use git worktree: `git worktree add /tmp/archibald-feature-branch feat/unified-operation-queue`

1. Read master: `git show master:archibald-web-app/backend/src/index.ts | sed -n '4514,4567p'` (quick-check)
   and `git show master:archibald-web-app/backend/src/index.ts | sed -n '2821,2857p'` (frequency)

2. Add GET /quick-check to sync-status.ts:
   - No auth required (master has none — this is called on app startup before login)
   - Check customer count + lastSync, product count + lastSync
   - Use DI-injected repository functions (getCustomerCount, getLastSyncTime, getProductCount, etc.)
   - Return: `{ success, data: { needsSync, needsInitialSync, customers: {count, lastSync, needsSync}, products: {count, lastSync, needsSync} } }`

3. Add POST /admin/sync/frequency to admin.ts (or create new route):
   - requireAdmin middleware (improve on master which has TODO for auth)
   - Body: `{ intervalMinutes: number }` — validate 5-1440
   - Call syncScheduler to update all intervals
   - Return: `{ success, intervalMinutes, message }`

4. Verify existing intervals endpoints match master:
   - GET /intervals should return intervals for all types
   - POST /intervals/:type should validate type and interval range (5-1440)
   - Check if requireAdmin is present on both (master requires it)

5. Wire new deps in server.ts if needed
  </action>
  <verify>`npm run build --prefix /tmp/archibald-feature-branch/archibald-web-app/backend` passes</verify>
  <done>quick-check and frequency endpoints implemented. Existing intervals verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests and verify</name>
  <files>archibald-web-app/backend/src/routes/sync-status.spec.ts, archibald-web-app/backend/src/routes/admin.spec.ts</files>
  <action>
1. Add tests for quick-check:
   - Returns needsSync=true when no data
   - Returns needsSync=false when recent data exists
   - Returns correct per-type breakdown

2. Add tests for admin/sync/frequency:
   - Valid interval → 200 with success
   - Invalid interval (< 5 or > 1440) → 400
   - Requires admin → 403

3. Verify existing intervals tests cover master behavior

4. Run full suite and commit:
   ```bash
   cd /tmp/archibald-feature-branch
   npm test --prefix archibald-web-app/backend
   npm run build --prefix archibald-web-app/backend
   git add [files individually]
   git commit -m "feat(03-01): implement quick-check and sync frequency endpoints

   - GET /sync/quick-check checks if initial sync needed
   - POST /admin/sync/frequency sets global sync interval
   - Verify existing intervals endpoints match master"
   ```
  </action>
  <verify>All backend tests pass. Type-check passes.</verify>
  <done>All endpoints tested. Build + tests pass.</done>
</task>

</tasks>

<verification>
- [ ] GET /api/sync/quick-check returns correct needsSync data
- [ ] POST /api/admin/sync/frequency works with requireAdmin
- [ ] Existing intervals endpoints match master behavior
- [ ] Backend type-check and tests pass
</verification>

<success_criteria>
- quick-check and frequency endpoints working
- Intervals endpoints verified
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-monitoring-endpoints/03-01-SUMMARY.md`
</output>
</content>
