---
phase: 05-stubs-partial-completion
plan: 01
type: execute
---

<objective>
Implement the 4 remaining sync monitoring endpoints to achieve parity with master.

Purpose: Complete the last stub/partial elements from the original 11 identified in Phase 1 audit. Most were already completed during Phases 2-4 (customer-interactive in Phase 2, health checks in Phase 4). These 4 monitoring endpoints are the final gap.
Output: 4 working monitoring endpoints with tests, querying real data from PostgreSQL sync_sessions table and BullMQ queue history.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-low-priority-debug-endpoints/04-03-SUMMARY.md

@archibald-web-app/backend/src/server.ts
@archibald-web-app/backend/src/routes/products.ts
@archibald-web-app/backend/src/routes/customers.ts
@archibald-web-app/backend/src/routes/sync-status.ts
@archibald-web-app/backend/src/db/migrations/002-shared-tables.sql
@archibald-web-app/backend/src/db/repositories/products.ts
@archibald-web-app/backend/src/db/repositories/customers.ts

**Tech stack available:** PostgreSQL via pg pool, Express DI pattern, Vitest, BullMQ
**Established patterns:** DI wiring in server.ts, route factories with typed deps, *.spec.ts colocated tests

**Constraining decisions:**
- 03-02: matchPricesToProducts and getHistorySummary wired as stubs (out of scope for this plan)
- 04-03: Health check endpoints unauthenticated (monitoring probes need no auth)

**Master reference for endpoint behavior:**

1. `GET /api/customers/sync/metrics` (master index.ts ~line 2249):
   - Returns: lastSyncTime, lastResult (success, customersProcessed, newCustomers, updatedCustomers, duration, error), totalSyncs, consecutiveFailures, averageDuration, health
   - Master uses in-memory `syncService.getMetrics()` — branch must derive from BullMQ queue history

2. `GET /api/products/sync/metrics` (master index.ts ~line 2742):
   - Returns: metrics (from db.getSyncMetrics()), history (last 10 syncs from db.getSyncHistory(10))
   - Branch has `shared.sync_sessions` table ready for this

3. `GET /api/products/sync-history` (master index.ts ~line 4245):
   - Returns: sessions array (startedAt, completedAt, duration, status, counts), count
   - Query param: ?limit=N (default 10)

4. `GET /api/products/last-sync` (master index.ts ~line 4287):
   - Returns: last sync session details or null if none found

**Database schema (shared.sync_sessions):**
```sql
CREATE TABLE IF NOT EXISTS shared.sync_sessions (
  id TEXT PRIMARY KEY,
  sync_type TEXT NOT NULL CHECK (sync_type = 'products'),
  started_at BIGINT NOT NULL,
  completed_at BIGINT,
  status TEXT NOT NULL CHECK (status IN ('running', 'completed', 'failed', 'partial')),
  total_pages INTEGER,
  pages_processed INTEGER,
  items_processed INTEGER,
  items_created INTEGER DEFAULT 0,
  items_updated INTEGER DEFAULT 0,
  items_deleted INTEGER DEFAULT 0,
  images_downloaded INTEGER DEFAULT 0,
  error_message TEXT,
  sync_mode TEXT NOT NULL CHECK (sync_mode IN ('full', 'incremental', 'forced', 'auto'))
);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync-sessions repository and wire product sync monitoring endpoints</name>
  <files>
    archibald-web-app/backend/src/db/repositories/sync-sessions.ts,
    archibald-web-app/backend/src/db/repositories/sync-sessions.spec.ts,
    archibald-web-app/backend/src/routes/products.ts,
    archibald-web-app/backend/src/routes/products.spec.ts,
    archibald-web-app/backend/src/server.ts,
    archibald-web-app/backend/src/server.spec.ts
  </files>
  <action>
    1. Create `db/repositories/sync-sessions.ts` with 3 functions following the existing repository pattern (functions that take DbPool as first arg):
       - `getSyncHistory(pool, limit = 10)` — query shared.sync_sessions ORDER BY started_at DESC LIMIT $1, return rows mapped to camelCase (id, syncType, startedAt, completedAt, status, totalPages, pagesProcessed, itemsProcessed, itemsCreated, itemsUpdated, itemsDeleted, imagesDownloaded, errorMessage, syncMode). Compute duration as `completedAt ? completedAt - startedAt : Date.now() - startedAt`. Format timestamps as ISO strings.
       - `getLastSyncSession(pool)` — call getSyncHistory(pool, 1) and return first element or null.
       - `getSyncStats(pool)` — query shared.sync_sessions to compute: totalSyncs (COUNT), lastSyncTime (MAX started_at), avgDurationMs (AVG of completed_at - started_at WHERE completed_at IS NOT NULL), successRate (COUNT WHERE status='completed' / total), recentHistory (last 10 sessions via getSyncHistory).

    2. Add unit tests in `sync-sessions.spec.ts` — insert test rows into shared.sync_sessions, verify getSyncHistory returns correct order and limit, verify getLastSyncSession returns most recent, verify getSyncStats computes correct aggregates. Use the existing test pool pattern from other repo specs.

    3. Add 3 new DI deps to ProductsRouterDeps:
       - `getSyncHistory?: (limit: number) => Promise<SyncSession[]>`
       - `getLastSyncSession?: () => Promise<SyncSession | null>`
       - `getSyncStats?: () => Promise<SyncStats>`

    4. Add 3 routes to the products router:
       - `GET /sync/metrics` — calls getSyncStats, returns 501 if not configured. No auth beyond the existing router-level authenticateJWT.
       - `GET /sync-history` — calls getSyncHistory with ?limit query param (default 10), returns 501 if not configured.
       - `GET /last-sync` — calls getLastSyncSession, returns 501 if not configured.
       IMPORTANT: Place these routes BEFORE the `/:productId` catch-all route to avoid path conflicts.

    5. Wire the 3 deps in server.ts under the products router DI section using sync-sessions repo functions.

    6. Add tests in products.spec.ts for the 3 new routes (mock deps returning sample data, test 200 response shape, test 501 when dep not configured).
  </action>
  <verify>
    npm run build --prefix archibald-web-app/backend (TypeScript compiles)
    npm test --prefix archibald-web-app/backend (all tests pass including new sync-sessions + products route tests)
  </verify>
  <done>
    3 product sync monitoring endpoints return real data from sync_sessions table. Tests cover happy path and 501 fallback. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement customers/sync/metrics endpoint from BullMQ queue history</name>
  <files>
    archibald-web-app/backend/src/routes/customers.ts,
    archibald-web-app/backend/src/routes/customers.spec.ts,
    archibald-web-app/backend/src/server.ts,
    archibald-web-app/backend/src/server.spec.ts
  </files>
  <action>
    1. Add a new optional DI dep to CustomersRouterDeps:
       - `getCustomerSyncMetrics?: () => Promise<CustomerSyncMetrics>`
       Where CustomerSyncMetrics = { lastSyncTime: string | null; lastResult: { success: boolean; customersProcessed: number; duration: number; error: string | null } | null; totalSyncs: number; consecutiveFailures: number; averageDuration: number; health: 'healthy' | 'degraded' }

    2. Add route `GET /sync/metrics` to the customers router — calls getCustomerSyncMetrics, returns 501 if not configured. Place BEFORE the `/:customerProfile` catch-all route.

    3. Implement the DI wiring in server.ts: create a function that queries BullMQ queue for completed/failed 'sync-customers' jobs to derive the metrics:
       - Use `queue.queue.getJobs(['completed', 'failed'], 0, 99)` to get recent sync-customers jobs
       - Filter to jobs where `job.data.type === 'sync-customers'`
       - Compute: totalSyncs (count), lastSyncTime (most recent job's finishedOn), lastResult (from most recent job's returnvalue/failedReason), consecutiveFailures (count failed from end until first success), averageDuration (avg of finishedOn - processedOn), health (consecutiveFailures < 3 ? 'healthy' : 'degraded')
       - This function is async and composable — pass it directly as the DI dep

    4. Add tests in customers.spec.ts for the new route: mock getCustomerSyncMetrics returning sample metrics, test 200 response shape matches master's contract, test 501 when dep not configured.
  </action>
  <verify>
    npm run build --prefix archibald-web-app/backend (TypeScript compiles)
    npm test --prefix archibald-web-app/backend (all tests pass including new customers sync metrics test)
  </verify>
  <done>
    GET /api/customers/sync/metrics returns metrics derived from BullMQ queue history. Response shape matches master's API contract (lastSyncTime, lastResult, totalSyncs, consecutiveFailures, averageDuration, health). Tests cover happy path and 501 fallback. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build --prefix archibald-web-app/backend` succeeds without errors
- [ ] `npm test --prefix archibald-web-app/backend` passes all tests (baseline: 860 + new tests)
- [ ] `npm run type-check --prefix archibald-web-app/frontend` still passes
- [ ] All 4 new endpoints respond correctly (sync/metrics, sync-history, last-sync for products; sync/metrics for customers)
- [ ] No regressions in existing tests
</verification>

<success_criteria>

- All 4 monitoring endpoints implemented and returning real data
- sync_sessions repository functions created with tests
- Response shapes match master's API contracts
- All verification checks pass
- Phase 5 complete — all original 11 stubs/partial elements resolved
</success_criteria>

<output>
After completion, create `.planning/phases/05-stubs-partial-completion/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Sync Monitoring Endpoints Summary

**[One-liner describing what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 5 complete, ready for Phase 6: Frontend Path Migration
</output>
