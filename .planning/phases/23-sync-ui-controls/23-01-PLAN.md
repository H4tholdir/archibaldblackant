---
phase: 23-sync-ui-controls
plan: 01
type: execute
---

<objective>
Create unified sync management UI with granular controls for all sync types.

Purpose: Centralized frontend interface for triggering and monitoring all syncs (customers, products, prices, orders).
Output: SyncControlPanel component with buttons for each sync type (full/incremental/auto modes), live status display, and health indicators.
</objective>

<context>
**Key files**: frontend/src/components/SyncControlPanel.tsx, SyncButton.tsx (refactor existing)

**Requirements**:
- Four sync type sections (Customers, Products, Prices, Orders)
- Each section: dropdown (Auto/Full/Incremental), sync button, status badge, last sync time, health indicator
- Live status updates via WebSocket or polling (GET /api/sync/status)
- Aggregated "Sync All" button (triggers all syncs in priority order via orchestrator)
- Visual feedback: color-coded status (idle/running/completed/error), progress bars

**Implementation**: Centralized UI component leveraging Phase 22 orchestrator status API.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SyncControlPanel component</name>
  <files>frontend/src/components/SyncControlPanel.tsx</files>
  <action>
    Create component with 4 sync type sections + "Sync All" button.
    Fetch status from GET /api/sync/status, poll every 5s during active syncs.
    Inline styles (Phase 10-06 pattern), banking app UX (color coding, clear labels).
  </action>
  <verify>UI renders with all sync types, status updates</verify>
  <done>SyncControlPanel component created. TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SyncControlPanel to Admin page or dashboard</name>
  <files>frontend/src/pages/AdminPage.tsx</files>
  <action>
    Integrate SyncControlPanel in admin section or create dedicated /sync-control route.
    Add navigation link in DashboardNav.
  </action>
  <verify>Panel accessible, navigation works</verify>
  <done>SyncControlPanel integrated. TypeScript passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unified sync UI with granular controls</what-built>
  <how-to-verify>
    1. Navigate to Sync Control Panel
    2. Verify 4 sync sections visible (Customers, Products, Prices, Orders)
    3. Test dropdown mode selection (Auto/Full/Incremental) for each
    4. Trigger individual syncs, verify status updates (idle→running→completed)
    5. Test "Sync All" button, verify priority execution (orders first)
    6. Check health indicators (green=healthy, red=degraded)
    7. Verify last sync timestamps display correctly
    8. Test concurrent sync requests, verify queueing behavior shown in UI

    Success: All controls functional, status updates real-time, health indicators accurate.
  </how-to-verify>
  <resume-signal>Type "approved"</resume-signal>
</task>

</tasks>

<success_criteria>
- Unified sync UI with 4 sync type controls
- Dropdown mode selection per type
- Live status updates (polling or WebSocket)
- Health indicators (green/red)
- "Sync All" button
- User verification passed
</success_criteria>

<output>
Create `.planning/phases/23-sync-ui-controls/23-01-SUMMARY.md`
</output>
