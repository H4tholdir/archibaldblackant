---
phase: 11-bootstrap
plan: 01
type: execute
---

<objective>
Create main.ts entry point that initializes all dependencies, calls createApp(), starts HTTP server, and handles graceful shutdown.

Purpose: The app cannot start without this. server.ts exports createApp(deps) but nothing calls it.
Output: Working main.ts that starts the server with all deps wired.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-02-23-full-parity-v1.2-design.md

@archibald-web-app/backend/src/server.ts
@archibald-web-app/backend/src/config.ts
@archibald-web-app/backend/src/db/pool.ts
@archibald-web-app/backend/src/operations/operation-queue.ts
@archibald-web-app/backend/src/operations/agent-lock.ts
@archibald-web-app/backend/src/bot/browser-pool.ts
@archibald-web-app/backend/src/sync/sync-scheduler.ts
@archibald-web-app/backend/src/realtime/websocket-server.ts
@archibald-web-app/backend/src/auth-utils.ts
@archibald-web-app/backend/src/password-cache.ts

**Tech stack:** PostgreSQL (pg), BullMQ (Redis), Puppeteer, Express
**Established patterns:** DI via function arguments, factory functions
**Constraining decisions:**
- server.ts exports createApp(deps: AppDeps) — do NOT modify server.ts
- Config loaded from config.ts via process.env
- All factories are already implemented, just need to be called
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main.ts with dependency initialization and server startup</name>
  <files>archibald-web-app/backend/src/main.ts</files>
  <action>
    Create main.ts that:
    1. Imports config, all factory functions, createApp
    2. Creates an async bootstrap() function that:
       a. Creates pool via createPool(config.database)
       b. Runs migrations via runMigrations(pool)
       c. Creates agentLock via createAgentLock()
       d. Creates operationQueue via createOperationQueue({ host: config.redis?.host, port: config.redis?.port }) — check config.ts for redis config, may need to add it
       e. Creates browserPool via createBrowserPool with config.puppeteer values
       f. Creates syncScheduler via createSyncScheduler(queue.enqueue, () => []) — getActiveAgentIds returns empty for now
       g. Creates wsServer via createWebSocketServer({ createWss: (server) => new WebSocketServer({ server }), verifyToken: verifyJWT })
       h. Gets passwordCache via PasswordCache.getInstance()
       i. Creates pdfStore as { store: async (buffer, filename) => filename, retrieve: async (filename) => null } — stub for now
       j. Creates sendEmail stub: async () => ({ messageId: 'not-configured' })
       k. Creates uploadToDropbox stub: async () => ({ path: 'not-configured' })
       l. Calls createApp(deps) with all the above
       m. Creates HTTP server via http.createServer(app)
       n. Initializes wsServer with the HTTP server
       o. Starts listening on config.server.port
       p. Starts syncScheduler with default intervals
    3. Sets up graceful shutdown on SIGTERM and SIGINT:
       a. Log shutdown start
       b. syncScheduler.stop()
       c. await queue.close()
       d. await wsServer.shutdown()
       e. await browserPool.shutdown()
       f. await pool.end()
       g. process.exit(0)
    4. Calls bootstrap() and catches errors

    Also update package.json:
    - "main": "dist/main.js"
    - "dev": "tsx watch src/main.ts"
    - "start": "node dist/main.js"
  </action>
  <verify>npm run build --prefix archibald-web-app/backend</verify>
  <done>main.ts compiles, package.json updated, TypeScript builds cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add main.ts unit test verifying bootstrap structure</name>
  <files>archibald-web-app/backend/src/main.spec.ts</files>
  <action>
    Create a basic test that verifies:
    1. main.ts exports a bootstrap function (or test it can be imported without side effects)
    2. The graceful shutdown handlers are registered

    Note: Since main.ts has side effects (starts server), the test should mock the dependencies and verify the wiring is correct. OR test the bootstrap function in isolation if it's exported.

    Consider: Export bootstrap() for testability, and only call it at module level with a guard:
    ```
    if (require.main === module || process.env.NODE_ENV !== 'test') {
      bootstrap().catch(...)
    }
    ```

    Run full test suite to ensure nothing is broken.
  </action>
  <verify>npm test --prefix archibald-web-app/backend && npm run build --prefix archibald-web-app/backend</verify>
  <done>main.ts tested, all existing tests still pass, TypeScript compiles</done>
</task>

</tasks>

<verification>
- [ ] `npm run build --prefix archibald-web-app/backend` passes
- [ ] `npm test --prefix archibald-web-app/backend` passes
- [ ] main.ts creates all dependencies and calls createApp()
- [ ] Graceful shutdown handlers registered for SIGTERM and SIGINT
- [ ] package.json points to main.ts/main.js
</verification>

<success_criteria>
- main.ts entry point created and compiles
- All dependencies initialized in correct order
- HTTP server starts on configured port
- Graceful shutdown cleanly stops all services
- Package.json updated to use main.ts
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-bootstrap/11-01-SUMMARY.md`
</output>
