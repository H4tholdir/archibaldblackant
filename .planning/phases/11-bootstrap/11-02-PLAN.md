---
phase: 11-bootstrap
plan: 02
type: execute
---

<objective>
Ensure migrations run automatically on startup and background services (sync scheduler, session cleanup) are properly initialized.

Purpose: Without migrations the DB tables don't exist. Without background services, no auto-sync happens.
Output: Migrations auto-run, sync scheduler starts with staggered intervals, session cleanup runs periodically.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@archibald-web-app/backend/src/main.ts
@archibald-web-app/backend/src/db/migrate.ts
@archibald-web-app/backend/src/sync/sync-scheduler.ts
@archibald-web-app/backend/src/interactive-session-manager.ts

**Tech stack:** PostgreSQL, BullMQ
**Established patterns:** DI, factory functions
**Constraining decisions:**
- Migrations are .sql files in db/migrations/
- runMigrations(pool) from db/migrate.ts handles execution
- SyncScheduler needs intervals: { agentSyncMs, sharedSyncMs }
- Master's staggered schedule: orders 10min, customers 30min, products 90min
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify migration runner works in main.ts bootstrap</name>
  <files>archibald-web-app/backend/src/main.ts</files>
  <action>
    Verify that runMigrations(pool) is called during bootstrap (should already be in 11-01).

    If not present, add it:
    1. Import runMigrations from './db/migrate'
    2. Call await runMigrations(pool) before createApp()
    3. Log: "Migrations complete"

    Also verify that the migration runner handles the case where migrations have already been applied (idempotent).

    Test by checking migrate.ts handles the system.migrations tracking table.
  </action>
  <verify>npm run build --prefix archibald-web-app/backend</verify>
  <done>Migration runner called during bootstrap, idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Configure sync scheduler with production intervals and operation processor</name>
  <files>archibald-web-app/backend/src/main.ts</files>
  <action>
    In main.ts bootstrap, after server starts listening:

    1. Start sync scheduler with production intervals:
       ```
       syncScheduler.start({
         agentSyncMs: 10 * 60 * 1000,   // 10 minutes for per-agent syncs
         sharedSyncMs: 30 * 60 * 1000,  // 30 minutes for shared syncs (products, prices)
       });
       ```

    2. Create and start the operation processor (BullMQ Worker):
       - Import createOperationProcessor from './operations/operation-processor'
       - Create processor with handler map connecting operation types to handler functions
       - The processor needs: pool, browserPool, agentLock, and handler factories
       - Wire handlers: sync-customers, sync-orders, sync-products, sync-prices, sync-ddt, sync-invoices, submit-order, send-to-verona, etc.
       - Check operation-processor.ts for the exact interface

    3. Add session cleanup interval:
       ```
       const SESSION_CLEANUP_INTERVAL = 60 * 60 * 1000; // 1 hour
       const cleanupInterval = setInterval(() => {
         // cleanup expired sessions if interactiveSessionManager exists
       }, SESSION_CLEANUP_INTERVAL);
       ```

    4. Add cleanupInterval to graceful shutdown (clearInterval)

    5. Log startup complete with port and enabled services
  </action>
  <verify>npm run build --prefix archibald-web-app/backend && npm test --prefix archibald-web-app/backend</verify>
  <done>Sync scheduler starts with intervals, operation processor created with handlers, session cleanup running</done>
</task>

</tasks>

<verification>
- [ ] `npm run build --prefix archibald-web-app/backend` passes
- [ ] `npm test --prefix archibald-web-app/backend` passes
- [ ] Migrations run on startup
- [ ] Sync scheduler starts with configured intervals
- [ ] Operation processor created with handler map
- [ ] Session cleanup interval running
- [ ] All cleanup in graceful shutdown
</verification>

<success_criteria>
- Migrations auto-run on startup
- Sync scheduler active with production intervals
- Operation processor processes queued jobs
- Session cleanup running hourly
- Graceful shutdown stops all intervals and services
</success_criteria>

<output>
After completion, create `.planning/phases/11-bootstrap/11-02-SUMMARY.md`
</output>
